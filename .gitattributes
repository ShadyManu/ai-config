# Prettier writes LF, and the adapter tests compare generated bytes against
# checked-in fixtures. Neither can depend on the platform's checkout conversion,
# so the working tree is LF everywhere, including on Windows.
* text=auto eol=lf
